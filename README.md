This repository proviides C-bindings for the webthing-rust library, thus allowing you to write webthings in C.
As an example, have a look at `examples/single-thing.c` and run `make ex=single-thing run`.
